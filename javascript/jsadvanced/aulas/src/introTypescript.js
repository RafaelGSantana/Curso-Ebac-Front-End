// boolean
// string
// tuple
let title;
[1, 'oi'];
// enum Name { chave = valor }
let Colors;
(function (Colors) {
  Colors.white = '#fff';
  Colors.red = '#f00';
  Colors.blue = '#00f';
  Colors.black = '#000';
}(Colors || (Colors = {})));
